/**
 * View model for a row in the System User list.
 * Derived from the CK type `System.Identity-2/User` (attributes `userName`, `email`).
 */
export interface SystemUser {
  rtId: string;
  userName: string;
  email: string;
}
