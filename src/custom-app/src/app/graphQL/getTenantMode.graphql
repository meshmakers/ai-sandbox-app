query GetTenantMode($first: Int) {
  runtime {
    systemTenantModeConfiguration(first: $first) {
      edges {
        node {
          rtId
          maintenanceLevel
          environmentMode
        }
      }
    }
  }
}
