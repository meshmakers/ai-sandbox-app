query GetAuditLog($first: Int) {
  runtime {
    systemAiAuditEvent(first: $first) {
      edges {
        node {
          rtId
          at
          eventType
          actorRef
          targetRef
          detail
        }
      }
    }
  }
}
